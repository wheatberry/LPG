

<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          
          <% if logged_in? %>
              <p class="hey"><em>Hey, <%= current_user.name %>'s in the Game!</em></p>
          <% else %>
              <p class="hey">LPG</p>
          <% end %>
        </div>
<div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class=<%= @home %> ><%= link_to 'home', root_path %></li>
            <li class=<%= @about %> ><%= link_to 'about', static_pages_about_path %></li>
            <li class=<%= @feedback %> ><%= link_to 'contact us', new_contact_path %></li>

              <% if logged_in? %>
                
                <li class=<%= @play %> ><%= link_to 'players', users_path %></li>
                <li class=<%= @pro %> ><%= link_to 'profile', @current_user %></li>
                <li class=<%= @logO %> ><%= link_to 'logout', session_path(@current_user), method: :delete %></li> 
              <% else %>
                <li class=<%= @signU %> ><%= link_to 'sign up', new_user_path %></li>
                <li class=<%= @logI %> ><%= link_to 'login', new_session_path %></li> 
              <% end %>
          </ul>
          
        </div>
        
      </div>
    </div>